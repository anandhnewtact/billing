from django.db import models

# Create your models here.

# class Common(models.Model):

#     created_date = models.DateTimeField('created_date', auto_now = True)
#     updated_date = models.DateTimeField('updated_date', auto_now = True)
#     active = models.BooleanField('active', default = True)

# class Settings(Common):

#     category_name = models.CharField('category_name', max_length = 200)
#     category_desc = models.CharField('category_desc', max_length = 200)
